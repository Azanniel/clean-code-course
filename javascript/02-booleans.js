// Booleanos
// Uma boa prática é escrever as variáveis como se fossem uma pergunta

// const userMajority = true;
const isUserOnMajority = true;

// Algumas flags que podem ajudar são:
// is, does, has

const isDisabled = true;
const userHasGroup = true;

// Exemplo em condicionais
if(isUserOnMajority) {
  //ToDo
}

// Booleans sempre como perguntas / semântico